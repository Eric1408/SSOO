#include <iostream>

#include "copyfile.h"
#include "usage.h"

int main(int argc, char* argv[]) {
  Usage(argc, argv);
  std::string option = argv[1];
  if (option == "-m") {
    
  } else if (option == "a") {

  }
 
  return 0;
}